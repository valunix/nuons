namespace Nuons.DependencyInjection.CodeFixes.Tests;

// Scaffolding holds the project layout in place until DI-side code fixes (NUDI003+) are added.
public class PlaceholderTests
{
	[Fact]
	public void Project_Builds() => Assert.True(true);
}
